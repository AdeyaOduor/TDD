const sum = require('./capitalize_string');

test('.charAt(0).toUpperCase', () => {
  expect(charAt(0)).toBe(toUpperCase);
});

test('.reverseString(str.substr(1))', () => {
  expect(str.substr(1)).toBe(reverse);
});